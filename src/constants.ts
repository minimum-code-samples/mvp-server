/**
 * Application-wide constant values.
 * @module constants
 */

export const API_KEY = "API_KEY";

/**
 * The configuration file.
 */
export const CONFIG_FILE = "config.toml";

/**
 * The configuration parameter that is the name of the error log file.
 */
export const ERROR_FILE = "ERROR_FILE";

/**
 * The configuration parameter that is the name of the log file.
 */
export const LOG_FILE = "LOG_FILE";

/**
 * The environment variable that stipulates the log level. Default level is specified by the constant `LOG_LEVEL_VALUE`.
 */
export const LOG_LEVEL = "LOG_LEVEL";

/**
 * The default logging level.
 */
export const LOG_LEVEL_VALUE = "info";
